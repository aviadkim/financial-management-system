import React from "react";
import { Box, Container, Heading } from "@chakra-ui/react";

const PreviewInterface: React.FC = () => {
  return (
    <Container maxW="container.xl" py={5}>
      <Box>
        <Heading>Preview Interface</Heading>
      </Box>
    </Container>
  );
};

export default PreviewInterface;
